apiVersion: v1
kind: Service
metadata:
  name: {{ include "betting-service.fullname" . }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "betting-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "betting-service.selectorLabels" . | nindent 4 }}